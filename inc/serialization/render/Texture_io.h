#pragma once

#include "ecs/Component.h"
#include "textures/Texture.h"

namespace lava
{
  template <class Archive>
  void save(Archive & ar, const CTexture & t)
  {
  }

  template <class Archive>
  void load(Archive & ar, CTexture & t)
  {
  }
}
